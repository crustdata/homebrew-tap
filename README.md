# homebrew-tap

Homebrew tap for the Crustdata macOS app (Apple Silicon, unsigned build).

## Install

```sh
brew install --cask crustdata/tap/crustdata
```

The fully-qualified name auto-trusts the tap on Homebrew 6.x+.

## Update

```sh
brew upgrade --cask crustdata
```
